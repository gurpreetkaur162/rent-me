<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gs.rentme">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".selectoption"
            android:label="@string/app_name" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".Home"
            android:label="@string/app_name" />
        <activity
            android:name=".signup"
            android:label="@string/app_name" />
        <activity
            android:name=".startingpage"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".settings" />
        <activity android:name=".update_prof" />
        <activity android:name=".Product_details" />
        <activity android:name=".MyPoductsActivity" />
        <activity android:name=".MyProductDetailsActivity" />
        <activity android:name=".ui.activities.UserListingActivity" />
        <activity android:name=".ui.activities.ChatActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".walkthrough" />
        <activity android:name=".slide_layout" />
        <activity android:name=".categories" />
        <activity android:name=".ViewCategoryProductActivity" />
        <activity android:name=".SearchActivity"></activity>
    </application>

</manifest>